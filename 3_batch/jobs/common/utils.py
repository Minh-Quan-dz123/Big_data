from datetime import datetime

def current_time() -> datetime:
    """
    Returns the current time.
    
    Returns:
        datetime: A datetime object of the current time.
    """
    return datetime.now()
